name: Deploy Proxy-Bridge to VPS

on:
  push:
    branches:
      - main
    paths:
      - 'proxy-bridge/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            # 1. Charger Node
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
            source ~/.bashrc 2>/dev/null || true

            # 2. Mise à jour du code
            cd /home/fish/queen-bee
            git fetch --all
            git reset --hard origin/main

            # 3. Installation et Build
            cd proxy-bridge
            npm install
            npm run build

            # 4. Relance "Propre" (Anti-Cache PM2)
            # On supprime pour forcer PM2 à relire l'ecosystem et le .env.bridge
            pm2 delete queenbee-api queenbee-socket || true
            
            # On relance via l'ecosystem
            pm2 start ecosystem.config.cjs --update-env
            pm2 save